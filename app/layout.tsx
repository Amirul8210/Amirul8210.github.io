import type { Metadata } from "next";
import { Inter, JetBrains_Mono } from "next/font/google";
import "./globals.css";

const inter = Inter({ 
  subsets: ["latin"],
  variable: '--font-inter',
});

const jetbrainsMono = JetBrains_Mono({
  subsets: ["latin"],
  variable: '--font-jetbrains-mono',
});

export const metadata: Metadata = {
  title: "Khandaker Amirul Islam | Backend Developer & AI Researcher",
  description: "Portfolio of Khandaker Amirul Islam, a Backend Developer and AI Researcher specializing in Python, DevOps, and Scalable Systems.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.variable} ${jetbrainsMono.variable} font-sans antialiased bg-primary text-foreground`}>
        {children}
      </body>
    </html>
  );
}
